/** @type {import('prettier').Config} */
const config = {
  plugins: ['prettier-plugin-jsdoc'],
  printWidth: 80,
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  endOfLine: 'lf',
}

module.exports = config
